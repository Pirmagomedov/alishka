name: Deploy_to_Prod

on: [repository_dispatch, workflow_dispatch]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
      -  name: Checkout
         uses: actions/checkout@v4
      
      -  name: Setup Node js
         uses: actions/setup-node@v4
         with:
           node-version: 20.x
           
      -  name: Build Project
         run: |
           sudo apt-get install rsync
           npm install
           npm run build

      -  name: Deployment on server
         uses: ./.github/actions/sshrsync-action
         with:
           privkey: ${{secrets.SSH_KEY}}
           host: "31.128.47.179"
           username: "root"
           hostname: "nainbot.ru"
           options: "--exclude=.git --exclude=node_modules --exclude=.git*"
           serverpath: "var/www/html"
#         uses: Pendect/action-rsyncer@v1.1.0
 #        
  #       env:
  #         DEPLOY_KEY: ${{secrets.SSH_KEY}}
  #         
  #       with:
  #         flags: "-avc --delete"
  #         src: "/build/"
  #         dest: "root@31.128.47.179:/var/www/html/"
          
